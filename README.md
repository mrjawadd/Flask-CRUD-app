# Flask CRUD Web App

Simple Flask application using SQLite that allows users to create, read, update, and delete items.

## How to Run Locally
1. Install dependencies:
   pip install -r requirements.txt
2. Run the app:
   python app.py
3. Open browser at http://127.0.0.1:5000

